// src/lib/types/exampleTypes.ts

/**
 * Example shared data type.
 * Define interfaces and types here that might be used
 * across different parts of the application (services, API routes, frontend components).
 */
export interface ExampleData {
  id: string;
  originalInput: string;
  processedData: string;
  timestamp: Date;
}

// Add other shared types related to this domain...